spring:
  application:
    name: backend

server:
  port: 8080

logging:
  level:
    net.likelion: DEBUG

springdoc:
  swagger-ui:
    path: /swagger
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /api-docs

jwt:
  secret:  ${JWT_SECRET}
  expiration: 86400000   # 24시간 (ms)